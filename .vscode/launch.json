{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "kotlin",
      "request": "launch",
      "name": "Kotlin Debug",
      "mainClass": "HelloWorldKt",
      "projectRoot": "${workspaceFolder}",
      "preLaunchTask": "Build Kotlin Debug",
      "classPath": ["${workspaceFolder}/build"]
    }
  ]
}
