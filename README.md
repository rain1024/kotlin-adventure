# Concurrent

## Chạy chương trình

```bash
# Chạy ví dụ về cách làm sai khi sử dụng coroutine
./gradlew runBadPracticeExample

# Chạy ví dụ về cách cải tiến khi sử dụng coroutine
./gradlew runImprovedExample
```


